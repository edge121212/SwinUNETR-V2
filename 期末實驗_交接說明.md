# 期末實驗交接說明（SwinUNETR-V2 + Decoder Attention Gate）

> 分支：`feat/decoder-attention-gate`。需要一張 NVIDIA GPU（4090 / 5070Ti 等）。

> **📌 給協助跑實驗的人 / AI 助理：你的任務**
> 1. 確認目前在 `feat/decoder-attention-gate` 分支（`git branch` 確認；不是的話 `git checkout feat/decoder-attention-gate`）。
> 2. 照「〇、從零開始」把環境裝好（含對應 CUDA 的 PyTorch）。
> 3. 跑 **E1（必做，全層 attention gate）**；時間/算力夠再跑 **E2a（選做）**。**E0 baseline 由另一台機器負責，這台不用跑。**
> 4. 跑完把各自 `aggregate_kfold.py` 印出的 **5-fold 測試平均 PZ / TZ / Mean Dice** 回報，並填入本檔最後的對照表。
> 5. **不要修改 `models/`、`main.py`、`test.py`、`run.sh`、`run.bat`**——只透過 `ATTN_LEVELS` 環境變數選實驗。有疑問先問人，不要自行改架構或調參。
> 6. 建議用 tmux/screen 長時間掛著跑；中途斷了重下同一條指令會自動續跑。

---

## 〇、從零開始（朋友第一次在新機器上跑）

需要一張 NVIDIA GPU。**共同步驟 1~3 兩個系統一樣，差別只在跑腳本（Linux 用 `run.sh`、Windows 用 `run.bat`）。**

### 🐧 Linux / WSL / macOS

```bash
# 1. clone（SSH；沒設 SSH key 就用下面 HTTPS 那行）
git clone git@github.com:edge121212/SwinUNETR-V2.git
# git clone https://github.com/edge121212/SwinUNETR-V2.git
cd SwinUNETR-V2

# 2. 切到我們的分支
git checkout feat/decoder-attention-gate

# 3. 裝環境（建議用虛擬環境）
#    3a. 先裝對應 CUDA 版本的 PyTorch（requirements.txt 沒含 torch）
pip install torch --index-url https://download.pytorch.org/whl/cu121   # 依機器 CUDA 版本調整
#    3b. 再裝其餘套件
pip install -r requirements.txt        # MONAI, nibabel, tqdm, einops, tensorboardX

# 4. 下載資料集（會放到 dataset/Task05_Prostate/）
./run.sh download Prostate

# 5. 跑 E1（主方法，全層 attention gate）
ATTN_LEVELS="1,2,3,4,5" ./run.sh kfold Prostate 500

# 6.（選做）E2a 消融
ATTN_LEVELS="1,2,3" ./run.sh kfold Prostate 500
```

### 🪟 Windows（用「命令提示字元 cmd」，不是 PowerShell）

```bat
:: 1. clone（Windows 多半用 HTTPS）
git clone https://github.com/edge121212/SwinUNETR-V2.git
cd SwinUNETR-V2

:: 2. 切到我們的分支
git checkout feat/decoder-attention-gate

:: 3. 裝環境
::   3a. 先裝對應 CUDA 的 PyTorch（requirements.txt 沒含 torch）
pip install torch --index-url https://download.pytorch.org/whl/cu121
::   3b. 其餘套件
pip install -r requirements.txt

:: 4. 下載資料集
run.bat download Prostate

:: 5. 跑 E1（主方法）—— set 設定的環境變數在「同一個 cmd 視窗」內有效
set ATTN_LEVELS=1,2,3,4,5
run.bat kfold Prostate 500

:: 6.（選做）E2a —— 直接覆寫成新值即可
set ATTN_LEVELS=1,2,3
run.bat kfold Prostate 500

:: 想回頭跑 baseline（無 AG）時，要先清掉變數：
::   set ATTN_LEVELS=
```

> **PowerShell 使用者**：設環境變數語法不同 —— 用 `$env:ATTN_LEVELS="1,2,3,4,5"`（清除用 `Remove-Item Env:\ATTN_LEVELS`），其餘指令相同。建議直接用 cmd 比較單純。

> **MONAI 版本提醒**：我們的 `models/swin_unetr.py` 是從 **MONAI 1.5.2** 複製來改的。
> `requirements.txt` 會從 git 裝最新版 MONAI；若 import 時報錯，請改裝 `pip install monai==1.5.2`。

> 之後若已 clone 過、只是要更新：`git fetch origin && git checkout feat/decoder-attention-gate && git pull`。

---

## 一、我們改了什麼（一句話）

在 **SwinUNETR-V2 的 decoder skip connection 上加 Attention Gate**（Attention U-Net 式的加性注意力），用來聚焦最難分割的 **PZ（周邊區）**。改造是**可開關、且逐層可選**的，baseline 與改造模型**共用同一份程式碼**，只差一個旗標。

### 動到的檔案
| 檔案 | 改動 |
|---|---|
| `models/swin_unetr.py`（**新增**，從 MONAI 1.5.2 複製來改）| 新增 `AttentionGate` 與 `AttnUnetrUpBlock` 兩個 class；`SwinUNETR` 新增參數 `attn_gate_levels`，依此決定哪幾個 decoder 層套 attention gate |
| `main.py` / `test.py` | import 改成 `from models.swin_unetr import SwinUNETR`；新增 `--attn_gate_levels` 旗標並傳進模型 |
| `run.sh` / `run.bat` | 新增環境變數 `ATTN_LEVELS` 開關；AG 實驗的 `runs/` 資料夾自動加 `_ag<levels>` 後綴，不會蓋到 baseline |

### 關鍵保證
- `attn_gate_levels` 為**空時，模型與原版 MONAI SwinUNETR 逐 key 完全相同**（已用 CPU 驗證），所以**現有 baseline 的 checkpoint 仍相容**。
- decoder 層編號：**1 = 最高解析（靠近輸出）… 5 = 最深/最粗**。
- 全程 **from scratch、不下載任何 pretrained 權重**（與論文一致）；`use_v2=True` 固定開啟。

---

## 二、三個實驗 + 指令

| 實驗 | 說明 | `ATTN_LEVELS` | 結果存到 |
|---|---|---|---|
| **E0 baseline** | V2，無 AG（對照組）| 不設 | `runs/prostate_fold{0..4}` |
| **E1 主方法** | V2 + AG（全 5 層）| `1,2,3,4,5` | `runs/prostate_ag12345_fold{0..4}` |
| **E2a 消融** | V2 + AG（只高解析層）| `1,2,3` | `runs/prostate_ag123_fold{0..4}` |

> **E0 目前已在另一台（5070Ti）跑中**，你這台**主要跑 E1、行有餘力再跑 E2a**。

### Linux / WSL / macOS（`run.sh`）
```bash
# E1：完整 5-fold，每 fold 500 epoch
ATTN_LEVELS="1,2,3,4,5" ./run.sh kfold Prostate 500

# E2a（選做）
ATTN_LEVELS="1,2,3" ./run.sh kfold Prostate 500

# 不帶 epoch 數 = 自動跑滿 ~40k iterations（論文設定，單 fold 約 10h）
ATTN_LEVELS="1,2,3,4,5" ./run.sh kfold Prostate
```

### Windows（`run.bat`）
```bat
set ATTN_LEVELS=1,2,3,4,5
run.bat kfold Prostate 500
```
（跑別的實驗前先 `set ATTN_LEVELS=` 清掉，或開新視窗。）

---

## 三、跑法說明（重要）

- **彈性 / 隨時可停**：kfold 會依序對 fold 0~4「訓練→測試」。已經有 `runs/<logdir>_test.log` 的 fold 會**自動跳過**；只訓練到一半（有 `model_final.pt`）的 fold 會**從 checkpoint 續訓**。所以中途關機 / 改天再跑都沒問題，重下同一條指令即可接續。
- **要不要跑滿你自己決定**：500 epoch ≈ 單 fold 3h（5-fold ≈ 15h）；跑滿 40k ≈ 單 fold 10h（5-fold ≈ 52h）。時間夠就跑 40k 衝「贏論文」，趕的話 500 epoch 就能交出完整對照。
- **測試用的權重**：是 `model.pt`（**驗證集最佳**模型，對齊論文用 val 挑模型的做法），不是最後一個 epoch。
- **每 25 epoch** 存一次 checkpoint（`val_every=25`）。

---

## 四、跑完要收集什麼（給報告）

每個實驗跑完，`run.sh` 會自動呼叫 `aggregate_kfold.py` 印出 5-fold 的 **PZ / TZ / Mean Dice 平均 ± 標準差**。把三個實驗的數字填進這張表：

| | PZ | TZ | Mean |
|---|---|---|---|
| 原論文 SwinUNETR-V2 | 0.6353 | 0.8457 | 0.7405 |
| **E0**（V2，無 AG）| ⏳ 跑中 | ⏳ 跑中 | ⏳ 跑中 |
| **E1**（V2 + AG 全層）| ? | ? | ? |
| **E2a**（V2 + AG 高解析）| ? | ? | ? |

> 狀態（2026-06-04）：**E0 baseline 正在 5070Ti 上跑（fold 0/5，約一半）**，尚無最終 5-fold 測試數字；
> 跑完後由各自 `aggregate_kfold.py` 的輸出填入。表中數字一律填「5-fold 測試集」平均（非 validation）。

報告要用的兩張圖/表：
- **5.1 vs 論文**：E1 對上論文 0.7405。
- **5.2 ablation**：E0 vs E1（只差 AG 一個變因）；E2a 再補「AG 擺哪有效」。
- **重點看 PZ**：我們的假設是 AG 主要把 PZ 拉上來，記得單獨比 PZ 那一欄。

> 每個 fold 的單獨分數在 `runs/<logdir>_test.log` 裡（含 per-class PZ/TZ Dice）。
